import React from 'react';

export default function MissionStrip() {
    return (
        <section className="mission-strip">
            <div className="mission-label">Our mission</div>
            <p className="mission-text font-serif-about">
                Modern organisations face a common constraint: revenue growth is limited not by <em>product quality</em> or <em>market demand</em> — but by the ability to <strong>execute sales reliably, visibly, and at scale.</strong>
            </p>
        </section>
    );
}